UPLOAD INSTRUCTIONS

This package fixes the 404 problem by adding a real root index.html file.

FILES:
- index.html
  Root menu page. This is what GitHub Pages, Netlify, and Cloudflare Pages look for first.
- gate-window.html
  Your Change Force Field — Gate Window page.
- boot-12-visual-reference.html
  Your CFS — Boot-12 Visual Reference page.

GITHUB PAGES:
1. Upload all three files to the root of the repository.
2. Make sure the file is named exactly index.html, lowercase.
3. Do not put the files inside an extra folder unless GitHub Pages is pointed to that folder.
4. Wait for Pages to redeploy.

NETLIFY DROP:
1. Upload the whole ZIP file or drag this whole folder.
2. Netlify should open index.html automatically.

CLOUDFLARE PAGES DIRECT UPLOAD:
1. Upload this folder or ZIP.
2. It should open index.html automatically.

If you only want one page live:
- Rename that page to index.html.
- Upload only that renamed file.


UPDATE: This bundle now includes full-year-gate-map.html and the root index.html links all three pages. Upload all files together to GitHub Pages.
